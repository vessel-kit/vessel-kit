import React from "react";
import styled from "@emotion/styled";
import { css } from "theme-ui";

const H1E = styled.h1(css({
  
}));

export function H1(props: React.PropsWithChildren<{}>) {
  return <h1></h1>;
}
